<% provide(:title, 'All Users') %>

<h1>All Users</h1>

<%= will_paginate %>

<ul class="users">
	<% @users.each do |user| %>
		<!-- Using Partial -->
		<%= render user %>	
	<% end %>
</ul>

<%= will_paginate %>